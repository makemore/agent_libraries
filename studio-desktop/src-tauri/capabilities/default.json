{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: remote Studio site only, no IPC surface beyond core defaults.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
