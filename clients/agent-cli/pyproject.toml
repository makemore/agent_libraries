[project]
name = "agentctl"
version = "0.1.0"
description = "Thin REST CLI for agent runtime and optional Studio integrations"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
studio = "agentctl:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
